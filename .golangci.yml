version: "2"

run:
  timeout: 3m

linters:
  default: none

  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - errorlint
    - misspell

formatters:
  enable:
    - gofmt
    - goimports